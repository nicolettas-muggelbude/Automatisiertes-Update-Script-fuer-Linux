# Update-Script Konfiguration
# Kopiere diese Datei nach config.conf und passe die Werte an
# cp config.conf.example config.conf

# Sprache / Language (auto|de|en|fr|es)
# 'auto' = Automatische Erkennung aus System-Locale / Automatic detection from system locale
LANGUAGE=auto

# E-Mail-Benachrichtigung aktivieren (true/false)
ENABLE_EMAIL=false

# E-Mail-Empfänger für Benachrichtigungen
EMAIL_RECIPIENT="admin@example.com"

# Verzeichnis für Log-Dateien
LOG_DIR="/var/log/system-updates"

# Automatischer Neustart bei Bedarf (true/false)
# WARNUNG: System wird automatisch neu gestartet, wenn Updates dies erfordern
AUTO_REBOOT=false

# Kernel-Schutz (true/false)
# Verhindert autoremove wenn zu wenige Kernel installiert sind
# Schützt vor versehentlichem Entfernen aller Fallback-Kernel
KERNEL_PROTECTION=true

# Minimale Anzahl stabiler Kernel (Standard: 3)
# Empfohlen: 3 = aktuell laufender Kernel + 2 Fallback-Versionen
MIN_KERNELS=3
