# Update-Script Konfiguration - Beispieldatei
#
# Installation:
# Option 1 (Empfohlen): ./install.sh ausführen (erstellt Config automatisch)
# Option 2 (Manuell): Diese Datei kopieren und anpassen
#
# Config-Location (seit v1.6.0 - XDG-konform):
# ~/.config/linux-update-script/config.conf
#
# Alte Location (deprecated, wird in v2.0.0 entfernt):
# ~/linux-update-script/config.conf

# Sprache / Language (auto|de|en|fr|es)
# 'auto' = Automatische Erkennung aus System-Locale / Automatic detection from system locale
LANGUAGE=auto

# E-Mail-Benachrichtigung aktivieren (true/false)
ENABLE_EMAIL=false

# E-Mail-Empfänger für Benachrichtigungen
EMAIL_RECIPIENT="admin@example.com"

# Verzeichnis für Log-Dateien
LOG_DIR="/var/log/system-updates"

# Automatischer Neustart bei Bedarf (true/false)
# WARNUNG: System wird automatisch neu gestartet, wenn Updates dies erfordern
AUTO_REBOOT=false

# Kernel-Schutz (true/false)
# Verhindert autoremove wenn zu wenige Kernel installiert sind
# Schützt vor versehentlichem Entfernen aller Fallback-Kernel
KERNEL_PROTECTION=true

# Minimale Anzahl stabiler Kernel (Standard: 3)
# Empfohlen: 3 = aktuell laufender Kernel + 2 Fallback-Versionen
MIN_KERNELS=3

# Upgrade-Check aktivieren (true/false)
# Prüft nach regulären Updates, ob Distribution-Upgrades verfügbar sind
ENABLE_UPGRADE_CHECK=true

# Automatisches Upgrade durchführen (true/false)
# WARNUNG: Kann Breaking Changes verursachen! Nur für erfahrene User
# Empfohlen: false (manuelle Upgrades mit --upgrade Flag)
AUTO_UPGRADE=false

# Upgrade-Benachrichtigungen per E-Mail (true/false)
# Sendet E-Mail wenn Distribution-Upgrade verfügbar ist
UPGRADE_NOTIFY_EMAIL=true

# Desktop-Benachrichtigungen aktivieren (true/false)
# Zeigt Popup-Benachrichtigungen für wichtige Events
ENABLE_DESKTOP_NOTIFICATION=true

# Notification-Dauer in Millisekunden (0 = Standard des Systems)
NOTIFICATION_TIMEOUT=5000
